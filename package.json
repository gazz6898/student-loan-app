{
  "name": "student-loan-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "lerna clean --yes && lerna bootstrap",
    "build": "lerna run build",
    "docker-prune": "docker compose down && docker volume prune -f && docker image prune -f",
    "setup": "npm run bootstrap && npm run build",
    "start:backend": "lerna run --scope=@student-loan-app/backend* start:dev",
    "start:frontend": "npm run start:dev --prefix=packages/frontend/web-app",
    "start": "docker compose up -d && npm run start:frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^4.0.0",
    "webpack": "^5.35.0",
    "webpack-cli": "^4.6.0",
    "webpack-dev-server": "^3.11.2"
  }
}
